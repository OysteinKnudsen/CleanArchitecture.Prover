namespace Application.UnitTests.Prøvegrupper;

public class PrøvegruppeServiceTests
{
    /*
     * Dette er bare starten på en testklasse med ett eksempel på hva som kan bli testet.
     * Poenget er å få demonstrert effekten av å ha tydelig definerte grensesnitt og at det er enkelt å teste.
     */
    
    [Fact]
    public void UpdateStatusAsync_WhenOutsidePrøveperiode_DoesNotUpdateStatus()
    {
        // TODO: Skriv en test som verifiserer at UpdateStatusAsync ikke oppdaterer status på en prøvegruppe som er utenfor prøveperioden.
        // Hint: Her må du mocke ut avhengighetene til PrøvegruppeService og verifisere at status ikke blir oppdatert. 
    }
}